import styled from '@emotion/styled';
import { vars } from 'variables/variables';

export const Logotype = styled.div`
  width: 300px;

  .logoTitle {
    color: ${vars.logoColor};
    font-family: 'Raleway';
    font-size: 26px;
    line-height: 1.19;
    text-align: center;
  }
`;
